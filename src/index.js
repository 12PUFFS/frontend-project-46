const genDiff = (filepath1, filepath2) => {
    // Пока заглушка - на следующих шагах будем реализовывать логику
    return `Difference between ${filepath1} and ${filepath2}`;
  };
  
  export default genDiff;